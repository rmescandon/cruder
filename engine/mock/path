package pkg

import (
	"one"
	"two"
	"three"
	"fmt"
)

type TheStruct struct {
	AnyValue	string
	OtherValue	int
}

var variable TheStruct

func theFunction() {
	err := do()
	if err != nil {
		fmt.Printf("error parsing parameters: %v\r\n", err)
		return
	}
}
